{% extends 'adminbase.html.twig' %}

{% block title %}Edit Skill{% endblock %}

{% block body %}
    <section class="admin d-flex flex-column align-items-center">
        <h1>ÉDITER UNE COMPÉTENCE</h1>

        {{ include('skill/_form.html.twig', {'button_label': 'Update'}) }}

        <a class="btn btn-primary" href="{{ path('skill_index') }}">Retour</a>

        {{ include('skill/_delete_form.html.twig') }}
    </section>
{% endblock %}
